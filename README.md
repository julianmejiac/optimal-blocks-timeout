# optimal-blocks-timeout
Script para calcular T_opt de bloques de votos usando Poisson
Input:
N: numero de votantes
k: numero de votos por bloque
Output:
T_opt: El timeout optimo (en minutos) bajo el modelo de Poisson. 
Se espera que k votos llegaran en el intervalo de T_opt minutos con probabilidad 99%
